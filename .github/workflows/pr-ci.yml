name: PR CI

on:
  pull_request:
    branches:
      - main

concurrency:
  group: pr-ci-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  rust:
    name: Rust checks and tests
    runs-on: [self-hosted, linux, g3-ci]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Show toolchain
        run: |
          rustc --version
          cargo --version

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Check macro crate
        run: cargo check --manifest-path macros/Cargo.toml

      - name: Check route transitions crate
        run: cargo check

      - name: Run macro tests
        run: cargo test --manifest-path macros/Cargo.toml

      - name: Run route transitions tests
        run: cargo test

      - name: Check whitespace
        run: git diff --check